"""
═══════════════════════════════════════════════════════════════════════
CONFIGURACIÓN DEL DASHBOARD
═══════════════════════════════════════════════════════════════════════
"""

import os
from pathlib import Path

# ───────────────────────────────────────────────────────────────────────
# RUTAS DEL PROYECTO
# ───────────────────────────────────────────────────────────────────────

BASE_DIR = Path(__file__).parent.parent
DATA_DIR = BASE_DIR / 'data'
PROCESSED_DIR = DATA_DIR / 'processed'
GEO_DIR = DATA_DIR / 'geo'

# ───────────────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE BASE DE DATOS (PostGIS)
# ───────────────────────────────────────────────────────────────────────

DB_CONFIG = {
    'host': os.getenv('DB_HOST', 'localhost'),
    'port': int(os.getenv('DB_PORT', 5434)),
    'database': os.getenv('DB_NAME', 'prototipo_salud'),
    'user': os.getenv('DB_USER', 'postgres'),
    'password': os.getenv('DB_PASSWORD', 'password')
}

# ───────────────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE STREAMLIT
# ───────────────────────────────────────────────────────────────────────

PAGE_CONFIG = {
    'page_title': 'Paradoja Extractivista Ecuador • Dashboard Analítico',
    'page_icon': '🛢️',
    'layout': 'wide',
    'initial_sidebar_state': 'expanded',
    'menu_items': {
        'Get Help': None,
        'Report a bug': None,
        'About': "Dashboard de análisis geoespacial para visualizar la paradoja extractivista en Ecuador. Fuentes: CONALI, INEC, MSP, MAATE."
    }
}

# ───────────────────────────────────────────────────────────────────────
# COLORES Y ESTILOS
# ───────────────────────────────────────────────────────────────────────

COLORS = {
    # Clusters (paleta profesional)
    'cluster_0': '#3b82f6',  # Azul - Sin petróleo
    'cluster_1': '#ef4444',  # Rojo - Alta actividad petrolera
    'cluster_2': '#10b981',  # Verde - Moderada
    'cluster_3': '#f59e0b',  # Naranja - Baja
    
    # Métricas (tonos neutros)
    'petroleo': '#dc2626',
    'salud': '#059669',
    'afro': '#7c3aed',
    'contaminacion': '#991b1b',
    
    # Mapas
    'sin_datos': '#f3f4f6',
    'borde': '#6b7280'
}

# ───────────────────────────────────────────────────────────────────────
# TEXTOS Y MENSAJES
# ───────────────────────────────────────────────────────────────────────

MENSAJES = {
    'titulo_principal': 'Paradoja Extractivista en Ecuador',
    'subtitulo': 'Análisis Geoespacial: Infraestructura Petrolera, Acceso a Salud y Población Afroecuatoriana',
    'descripcion_corta': 'Las parroquias con actividad petrolera tienen 33% menos acceso a servicios de salud.',
    'footer': 'TFM - Máster en Análisis de Datos Masivos | 2025'
}

# ───────────────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE MAPAS
# ───────────────────────────────────────────────────────────────────────

MAP_CONFIG = {
    'center': [-1.8312, -78.1834],  # Centro de Ecuador
    'zoom_start': 6,
    'tiles': 'CartoDB positron',
    'attr': 'CartoDB'
}

# ───────────────────────────────────────────────────────────────────────
# MÉTRICAS CLAVE
# ───────────────────────────────────────────────────────────────────────

METRICAS_CLAVE = {
    'total_parroquias': 1236,
    'parroquias_con_petroleo': 50,
    'total_pozos': 6287,
    'total_contaminacion': 7850,
    'diferencia_salud': -33  # % menos en zonas petroleras
}

